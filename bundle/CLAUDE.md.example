# CLAUDE.md — project context (example)

**Customize this file for your repository.** You may rename or merge into an existing `CLAUDE.md`.

Installed from: `git@github.com:Katestheimeno/workflow-kit.git` (workflow-kit) — exact version is in `.claude/WORKFLOW_KIT` after install.

---

## Read this first

**Before reading anything else in this file, read `.claude/CLAUDE_ENTRYPOINT.md`.**

That file contains the mandatory Task Checkpoint Protocol that must run before every coding action. Non-negotiable.

---

## Project identity (replace with your project)

- **Name:**
- **Stack / runtime:**
- **What this repo does (one paragraph):**

## Where project rules live

- Add links or paths to your own conventions (e.g. `CONTRIBUTING.md`, `docs/`, language-specific style guides).
- The workflow kit only installs the **task checkpoint** under `.claude/`; it does not ship domain-specific rules.

## Related

- `.claude/CONTEXT_MAP.md` — current snapshot; update when features complete.
- `.claude/tasks/MASTER_PLAN.md` — active feature and queue.
